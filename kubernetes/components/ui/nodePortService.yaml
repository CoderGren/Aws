apiVersion: v1
kind: Service
metadata:
  name: ui-node-port
  namespace: ui
  labels:
    app.kubernetes.io/name: ui
    app.kubernetes.io/instance: ui
    app.kubernetes.io/component: service
spec:
  type: NodePort
  ports:
    - port: 8080
      nodePort: 30001
      name: external-http
  selector:
    app.kubernetes.io/name: ui
    app.kubernetes.io/instance: ui
    app.kubernetes.io/component: service